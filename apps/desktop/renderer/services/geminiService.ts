/** Studio story generation — server/Gemini integration surface. */
export async function generateStoryStream(_input: unknown): Promise<{ content: string; report: unknown }> {
  return { content: "", report: null };
}
