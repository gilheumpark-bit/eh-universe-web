# 구잔재 삭제 목록 (2026-06-12)

## 기준

이번 정리는 Loreguard 신버전 기준으로 사용 경로가 끊긴 데스크톱, Code Studio, Network, 공개 Universe/Archive 계열 잔재를 대상으로 삼았다.

- 삭제: 현재 앱에서 import 경로가 없거나, 제거된 제품군의 단독 라우트/API로 남은 파일
- 보류: 아직 `/studio`, 번역, 집필 IDE 기능 또는 테스트에서 참조되는 파일
- 살림: 이름은 구버전처럼 보여도 신버전 Loreguard 집필 기능에 실제로 쓰이는 파일

## 삭제 완료

| 경로 | 분류 | 근거 |
|---|---|---|
| `src/app/desktop/page.tsx` | 데스크톱 구버전 라우트 | 부모 폴더에 별도 백업 완료. 신버전 Loreguard 흐름과 분리 |
| `src/components/desktop/CommandPalette.tsx` | 데스크톱 전용 UI | `src/app/desktop/page.tsx` 외 import 없음 |
| `src/components/desktop/InPageSearch.tsx` | 데스크톱 전용 UI | `src/app/desktop/page.tsx` 외 import 없음 |
| `src/components/desktop/LocalAISettings.tsx` | 데스크톱 전용 UI | `src/app/desktop/page.tsx` 외 import 없음 |
| `src/components/desktop/Toaster.tsx` | 데스크톱 전용 UI | `src/app/desktop/page.tsx` 외 import 없음 |
| `src/app/api/code/autopilot/route.ts` | Code Studio 잔재 API | `/api/code/autopilot` 호출처 없음 |
| `src/app/api/npm-search/route.ts` | Code Studio/NPM 탐색 잔재 API | `/api/npm-search` 호출처 없음 |
| `src/app/api/network-agent/ingest/route.ts` | Network 잔재 API | Network 제품 제거 방침에 따른 외부 공개 엔드포인트 제거 |
| `src/app/api/network-agent/search/route.ts` | Network 잔재 API | Network 제품 제거 방침에 따른 외부 공개 엔드포인트 제거 |
| `src/components/studio/NetworkFeedWidget.tsx` | Network 잔재 UI | 구 Studio에서 이미 비활성 주석 처리된 위젯 import 제거 후 삭제 |
| `src/components/studio/ShareToNetwork.tsx` | Network 잔재 UI | 원고 탭의 네트워크 공유 버튼 제거 후 삭제 |
| `src/lib/hooks/useNetworkAgent.ts` | Network 잔재 클라이언트 | 삭제된 `/api/network-agent/*` 호출 전용 hook |
| `src/lib/network-agent-client.ts` | Network 잔재 클라이언트 | 삭제된 `/api/network-agent/*` 호출 전용 클라이언트. origin 정규화는 번역 env 모듈로 이관 |
| `src/lib/network-agent-auth.ts` | Network 잔재 라이브러리 | Network 전용 인증 헬퍼. 외부 참조 없음 |
| `src/lib/network-agent-cors.ts` | Network 잔재 라이브러리 | Network 전용 CORS 헬퍼. 외부 참조 없음 |
| `src/lib/network-firestore.ts` | Network 잔재 라이브러리 | Network 전용 Firestore 헬퍼. 외부 참조 없음 |
| `src/lib/network-permissions.ts` | Network 잔재 라이브러리 | Network 전용 권한 헬퍼. 외부 참조 없음 |
| `src/lib/network-translations.ts` | Network 잔재 라이브러리 | Network 전용 라벨. 전용 테스트 외 참조 없음 |
| `src/lib/network-types.ts` | Network 잔재 타입 | Network 전용 타입. 전용 모듈/테스트 외 참조 없음 |
| `src/lib/network/*` | Network 잔재 라이브러리 | app/network, components/network 부재. 전용 모듈 내부 참조만 남아 삭제 |
| `src/lib/network/__tests__/helpers.test.ts` | Network 잔재 테스트 | 삭제된 Network helper 전용 테스트 |
| `src/lib/__tests__/network-agent-auth.test.ts` | Network 잔재 테스트 | 삭제된 Network auth 헬퍼 전용 테스트 |
| `src/lib/__tests__/network-permissions.test.ts` | Network 잔재 테스트 | 삭제된 Network permissions 전용 테스트 |
| `src/lib/__tests__/network-translations.test.ts` | Network 잔재 테스트 | 삭제된 Network translations 전용 테스트 |
| `src/lib/__tests__/network-types.test.ts` | Network 잔재 테스트 | 삭제된 Network types 전용 테스트 |
| `src/lib/__tests__/sanitize.test.ts` | Network 잔재 테스트 | 삭제된 Network sanitize helper 전용 테스트 |
| `docs/api/` | 생성 산출물 | gitignored TypeDoc output. 2,068개 파일, 약 23MB |
| `src/hooks/useCodeStudioKeyboard.ts` | Code Studio 잔재 hook | Code Studio 본체 부재, 전용 테스트 외 참조 없음 |
| `src/hooks/useSessionRestore.ts` | Code Studio 잔재 hook | `code-studio-last` IndexedDB 복원 전용, 전용 테스트 외 참조 없음 |
| `src/hooks/__tests__/useCodeStudioKeyboard.test.ts` | Code Studio 잔재 테스트 | 삭제된 hook 전용 테스트 |
| `src/hooks/__tests__/useSessionRestore.test.tsx` | Code Studio 잔재 테스트 | 삭제된 hook 전용 테스트 |
| `src/components/studio/SettingsView.tsx` | Code Studio 잔재 UI | 개발자 탭의 Code Studio 토글 제거, 개발자 모드만 유지 |
| `src/contexts/UserRoleContext.tsx` | Code Studio 잔재 hook export | `useCanAccessCodeStudio` 외부 참조 없음 |
| `src/components/SkeletonLoader.tsx` | Archive/Network/Code Studio 잔재 UI | 미사용 `CodeStudioSkeleton`, `ArchiveSkeleton`, `NetworkSkeleton` 제거 |
| `src/lib/actions/action-registry.ts` | Code Studio/Network 잔재 액션 | 팔레트 카탈로그에서 `code:*`, `network:*` 액션 및 영역 타입 제거 |
| `src/lib/keyboard/keyboard-manager.ts` | Code Studio/Network/Desktop 잔재 영역 | 키바인딩 영역에서 삭제된 라우트 영역 제거 |
| `src/lib/translator-constants.ts` | Network 잔재 탭 | Translation Studio workspace 탭에서 `network` 제거 |
| `src/components/translator/TranslatorStudioApp.tsx` | Network 잔재 탭 | 저장된 `network` 탭 복원과 미연결 scroll ref 제거 |
| `src/app/offline/page.tsx` | Code Studio 잔재 문구 | 오프라인 안내의 cached page 목록 정정 |
| `src/app/terms/page.tsx` | Code Studio/Network 잔재 문구 | 현행 서비스 구성에서 Code Studio/Network Community 제거 |
| `src/app/codex/*` | 공개 Codex 라우트 | 홈/헤더/단축키/테스트 참조 제거 후 단독 라우트 삭제 |
| `src/components/codex/*` | 공개 Codex UI | SettingsView의 도메인 선택 UI 제거 후 외부 참조 없음 |
| `src/app/reports/*` | 공개 보고서 라우트 | 홈/헤더/sitemap 참조 제거 후 공개 보고서 라우트 삭제 |
| `src/app/reference/*` | 공개 Reference 라우트 | 홈/헤더/sitemap 참조 제거 후 단독 라우트 삭제 |
| `src/app/rulebook/*` | 공개 Rulebook 라우트 | 홈/헤더/sitemap 참조 제거 후 단독 라우트 삭제 |
| `src/app/tools/*` | 공개 Tools 라우트 | 홈/헤더/sitemap 참조 제거 후 standalone 도구 라우트 삭제 |
| `src/components/tools/*` | 공개 Tools UI | `/tools` 라우트 삭제 후 외부 참조 없음 |
| `src/lib/tools/noa-tower/*` | 공개 Tools 엔진 | `/tools/noa-tower` 삭제 후 외부 참조 없음 |
| `src/lib/tool-links.ts` | 공개 Tools 링크 레지스트리 | Header/tools index 제거 후 외부 참조 없음 |
| `src/app/world/[id]/page.tsx` | 공개 Universe 상세 라우트 | 공개 world route 제거. Studio 내부 WorldSimulator는 보존 |
| `src/lib/articles*.ts` | 공개 Archive/Reports 데이터 | reports/reference/rulebook/codex 공개 라우트 삭제 후 외부 참조 없음 |
| `src/lib/__tests__/articles-reports.test.ts` | 공개 기사 데이터 테스트 | 삭제된 `articles-reports` 전용 테스트 |
| `src/components/studio/tabs/WritingTabContainer.tsx` | 구 Studio 미사용 컨테이너 | 파일 자기 자신 외 참조 0건 |

## 참조 정리 완료

| 경로 | 정리 내용 |
|---|---|
| `docs/API.md` | 삭제된 Network, NPM search, Code autopilot 엔드포인트 제거 |
| `docs/security/auth-matrix.md` | 삭제된 API 라우트의 인증/CSRF 매트릭스 행 제거 |
| `docs/NOA_POLICY.md` | 삭제된 `/api/code/autopilot` 기준 제거, 적용 라우트 수 8개에서 7개로 정정 |
| `docs/adr/0009-observability-standard.md` | 삭제된 Network search API SLO 행 제거 |
| `src/components/studio/StudioTabRouter.tsx` | `NetworkFeedWidget` 동적 import 제거 |
| `src/components/studio/tabs/ManuscriptTab.tsx` | 네트워크 공유 버튼/모달 제거 |
| `src/components/studio/tabs/__tests__/ManuscriptTab.test.tsx` | 삭제된 `ShareToNetwork` mock 제거 |
| `src/lib/translator-env-status.ts` | `network-agent-client` 의존 제거, origin 정규화 로컬화 |
| `src/components/translator/EnvStatusBar.tsx` | 네트워크 브릿지 문구를 Universe 연결 상태로 정정 |
| `src/components/Header.tsx` | Explore/Tools 공개 메뉴 제거, 정보 메뉴로 축소, 좌상단 로고 표기를 `LG`로 정정 |
| `src/app/page.tsx` | reports/codex/rulebook/reference 홈 허브 제거, CTA를 Studio/Translation 중심으로 정리 |
| `src/app/sitemap.ts` | 삭제된 공개 라우트 제거 |
| `src/components/GlobalShortcuts.tsx` | `/codex` 글로벌 진입 단축키 제거, 단축키 도움말만 유지 |
| `src/lib/actions/action-registry.ts` | `codex` action area 및 Codex 액션 제거 |
| `src/lib/keyboard/keyboard-manager.ts` | `codex` key area 제거 |
| `src/components/studio/PlanningView.tsx` | `/network/new` 행성 등록 버튼 제거 |
| `src/components/studio/OSDesktop.tsx` | `/archive` 유니버스 이동 링크 제거 |
| `src/components/studio/SettingsView.tsx` | 삭제된 Codex UI selector import/렌더 제거 |
| `src/app/docs/page.tsx` | 삭제된 `/tools/style-studio` 레거시 URL 문구 제거 |
| `src/lib/web-features/structured-data.ts` | `/archive/{slug}` JSON-LD URL과 Archive/Code 중심 feature copy 제거 |
| `src/lib/desktop/*` | `src/lib/writing-workspace/*`로 이관. 집필 IDE 유틸임을 경로명으로 정정 |

## 삭제 보류

| 경로/영역 | 상태 | 보류 이유 |
|---|---|---|
| `src/lib/writing-workspace/*` | 보류/살림 | `TabWriting`, `RevisionPanel`, `MemoPanel` 등 Loreguard 집필 기능에서 실제 사용 중 |
| `src/lib/ai/codex-prompts`, `src/lib/ai/codex-domain-storage.ts` | 삭제/대체 완료 | `creative-domain-prompts`, `creative-domain-storage`로 대체 |
| `src/components/world-simulator/*`, `src/components/WorldSimulator.tsx` | 보류/살림 | 공개 `/world/[id]`는 삭제했지만 Studio 내부 세계관 시뮬레이터에서 사용 중 |
| `src/app/api/lsp` | 보류 | `LSPTokenSection`, completion gap orchestrator에서 사용 |
| `src/app/api/fetch-url` | 보류 | Translation Studio URL import 경로에서 사용 |
| `src/app/api/local-proxy` | 보류 | AI provider 로컬 프록시 경로에서 사용 |

## 살릴 대상

- `src/lib/writing-workspace/writing-stats.ts`: 집필 통계 계산에 사용
- `src/lib/writing-workspace/revision-analysis.ts`: 수동 집필/수정 워크플로우에 사용
- `src/lib/writing-workspace/export-spec.ts`: 원고 내보내기 기능에 사용
- `src/lib/scene-sheet/*`: 신버전 Loreguard 씬시트 계층화 기준으로 사용
- `src/components/loreguard/*`: 신버전 Loreguard shell, 탭, 집필 IDE UI 본체

## 후속 정리 후보

1. `StudioShell`은 현재 Loreguard real provider envelope 역할을 유지한다. provider 추출 후 구 chrome(OSDesktop/MobileStudioView/StudioMainContent) 분기를 별도 삭제한다.
2. `src/components/studio/*`는 파일명 참조 0 기준으로 1차 스캔 완료. 현재 추가 미사용 확정 파일은 없음.
3. Code Studio 관련 문서, 테스트, 타입 잔재는 별도 라운드에서 `code-studio` 문자열 기준으로 재검증한다.
4. `docs/api/`는 삭제 완료. 필요하면 `docs/README.md` 기준으로 TypeDoc을 재생성한다.

## 검증

- `npx tsc --noEmit` 통과
- `npx jest src/__tests__/navigation.test.ts src/components/__tests__/GlobalShortcuts.test.tsx src/lib/actions/__tests__/action-registry.test.ts src/lib/keyboard/__tests__/keyboard-manager.test.ts src/lib/writing-workspace/__tests__/writing-stats.test.ts src/lib/writing-workspace/__tests__/context-block.test.ts src/lib/writing-workspace/__tests__/export-spec.test.ts src/lib/writing-workspace/__tests__/revision-analysis.test.ts --runInBand --cacheDirectory .jest-cache`
  - 8 suites / 107 tests 통과
- `npx jest src/components/__tests__/Header.test.tsx --runInBand --cacheDirectory .jest-cache`
  - 1 suite / 4 tests 통과
  - 기존 Jest 설정 경고 `Unknown option "setupFilesAfterEach"`는 재현됨

## 1~4 추가 정리분 용량

- 공개 Codex/Reports/Reference/Rulebook/Tools/World 라우트, `articles*`, tool/codex UI, `WritingTabContainer` 삭제분: 약 1,324.83KB
- `src/lib/desktop` → `src/lib/writing-workspace` 36파일, 약 176.87KB는 경로 이관이라 순감소로 계산하지 않음

## 백업

- 데스크톱 라우트 백업: `C:\Users\sung4\OneDrive\바탕 화면\EH\_backup_desktop_2026-06-12\src\app\desktop\page.tsx`

## 1~4 권장안 진행 결과 (추가)

### 1. StudioShell 구 chrome 분기 제거

- `src/app/studio/StudioShell.tsx`의 렌더 출력을 Loreguard provider envelope + `children` 중심으로 축소.
- 구 `OSDesktop`, `MobileStudioView`, `StudioMainContent`, 모바일 드로어/탭바, Zen overlay, Rename dialog 동적 렌더 분기를 제거.
- `Ctrl+1~6`은 `loreguard:tab` 이벤트만 디스패치하도록 정리.
- `Ctrl+7`은 `loreguard:open-history`, `Ctrl+8`은 `loreguard:open-settings`로 연결.

### 2. Studio 구 탭/패널 추가 삭제

- 삭제 확정: 구 `StudioTabRouter`, `WorldTab`, `CharacterTab`, `RulebookTab`, `HistoryTab`, `ManuscriptTab`, `VisualTab`, `WritingTabInline`, `SceneSheetTab`.
- 삭제 확정: 구 chrome UI `OSDesktop`, `MobileStudioView`, `MobileTabBar`, `MobileDrawer`, `LastTaskCard`, `ZenOverlays`, `ZenTweaksPanel`, `RenameDialog`.
- 삭제 확정: 미사용 보조 패널 `DebuggerPanel`, `ForeshadowLedger`, `LongArcGraph`, `MetaContextPanel`, `MultiCursorBar`, `DropoutHeatmap`, `ReaderProfilePanel`, `SemanticDiffPanel`, `SnippetPalette`.
- 삭제 확정: 구 writing mode 섹션 `AdvancedModeSection`, `CanvasModeSection`, `InputDockSection`, `MobileOverlaySection`, `ModeLoadingPlaceholder`, `RefineModeSection`.
- 삭제 확정: 구 world 보조 섹션 `EndingLockSection`, `WorldFactChatFill`.
- 빈 디렉터리 제거: `completion-gap`, `meta-context`, `multi-cursor`, `novel-ide`, `semantic-diff`, `snippets`.

### 3. Code Studio / Reports 잔재 정리

- `src/lib/browser/advanced-apis.ts`의 `/code-studio` preload 제거.
- `src/lib/modals/modal-manager.tsx`의 `code-studio:*`, `network:confirm` ModalId/Payload 제거.
- `src/lib/translations-{ko,en,ja,zh}.ts`의 미사용 top-level `archive`, `archivePage`, `codeStudio` 블록 제거.
- `src/lib/changelog-data.ts`의 공개 Code Studio 전용 changelog entry 제거, scope union에서 `code-studio`/`network` 제거.
- `src/lib/ai/writing-agent-registry.ts`의 호출처 0건 구 네트워크 검색 에이전트/검색 가드 3종 제거.
- `src/data/reports/*`, `src/lib/report-categories.ts`, `src/lib/__tests__/report-categories.test.ts` 삭제.

### 4. Codex 생성 프롬프트 계열 rename

- `src/lib/ai/codex-prompts/*` → `src/lib/ai/creative-domain-prompts/*`.
- `src/lib/ai/codex-domain-storage.ts` → `src/lib/ai/creative-domain-storage.ts`.
- 타입/상수/함수명 정리:
  - `CodexDomain*` → `CreativeDomain*`
  - `ALL_CODEX_DOMAINS` → `ALL_CREATIVE_DOMAINS`
  - `getStoredCodexDomain` → `getStoredCreativeDomain`
  - `setStoredCodexDomain` → `setStoredCreativeDomain`
  - storage key `noa.codex.domain` → `loreguard.creative.domain`
- `codex-structured-json` agent id → `creative-structured-json`.
- `src/lib/modals/modal-manager.tsx`의 `codex:*` modal id는 제거 완료.

## 1~4 추가 용량

- Studio 구 chrome/탭/패널 삭제: 55파일, 약 706.10KB.
- Reports 데이터/카테고리 삭제: 55파일, 약 425.81KB.
- 이번 추가 삭제 합계: 110파일, 약 1,131.91KB.
- 현재 tracked 삭제 누적: 232파일, 약 2.88MB.
- 현재 `src/components/studio`: 181파일, 2,578,273 bytes(약 2,517.84KB).
- `creative-domain-prompts`는 rename/보존 대상이라 순감소 계산에서 제외.

## 1~4 추가 검증

- `npx tsc --noEmit` 통과.
- `npx jest src/__tests__/navigation.test.ts src/components/__tests__/GlobalShortcuts.test.tsx src/components/__tests__/Header.test.tsx src/lib/actions/__tests__/action-registry.test.ts src/lib/keyboard/__tests__/keyboard-manager.test.ts src/lib/modals/__tests__/modal-manager.test.tsx src/lib/__tests__/changelog-data.test.ts src/lib/creative/__tests__/work-receipt-journal.test.ts src/lib/writing-workspace/__tests__/global-search-index.test.ts src/services/__tests__/geminiService.test.ts --cacheDirectory .jest-cache`
  - 10 suites / 158 tests 통과.
  - 기존 Jest 설정 경고 `Unknown option "setupFilesAfterEach"`는 재현됨.
- `src` 스캔 결과: 구 Code Studio/Codex/Network 표면 및 구 네트워크 검색 에이전트 ID 잔재 0건.
- 공개 `src/app` 라우트 확인: `about`, `ai-disclosure`, `api`, `changelog`, `cookies`, `copyright`, `docs`, `offline`, `payment`, `preview`, `pricing`, `privacy`, `refund`, `status`, `studio`, `terms`, `translation-studio`, `verify`, `welcome`.

## 남은 범위 (소스 외 문서)

- `src` 기준 실행 코드/공개 라우트의 Codex/Code Studio 잔재는 0건까지 정리.
- 루트 지침/README/ADR/과거 감사 문서에는 `Code Studio`, `Network`, `Archive`, `Codex` 역사 기록과 stale 지침이 다수 남아 있음.
- 이 문서군은 제품 코드 삭제와 달리 정책/이력 문서 성격이 섞여 있어, 다음 라운드에서 "보존할 역사 기록"과 "현행 지침에서 제거할 stale 문구"를 분리해야 함.
- 우선 정리 후보:
  - `AGENTS.md`, `CLAUDE.md`, `README.md`: 현행 Loreguard/Translation 중심 구조로 재정렬 필요.
  - `docs/adr/0003-keymap-and-codex-domain-selector-mount.md`: Codex domain selector ADR로 현재 rename 이후 stale.
  - `docs/adr/0004-grade-namespace-isolation.md`: CodeGrade 역사 문맥 보존 여부 재판단 필요.
  - `docs/feature-inventory.md`, `docs/bundle-report.md`, 과거 release/bug/audit 문서: 이력 문서로 보존할지 archive 폴더로 격리할지 결정 필요.

## 6월 10일 이전 데이터 추가 삭제

2026-06-12 추가 정리:

- 루트의 2026-05-07 생성 로컬 산출물 27개 삭제.
  - `check-*.png`, `ehuniverse-*.png`, `studio-error.png`
  - `_build_*.py`, `_modoo_*.mjs`, `_capture_evidence.mjs`, `_demo_noa_runtime.mjs`
  - `_patent_extracted.txt`, `playwright-results.json`
  - 모두 git 추적 대상이 아닌 임시 산출물.
  - 삭제 용량: 약 15.5MB.
- 6월 10일 이전 stale 문서/산출물 삭제.
  - `docs/README.md`
  - `docs/FEATURE_FLAGS.md`
  - `docs/QA-bug-report-checklist.md`
  - `docs/feature-inventory.md`
  - `docs/bundle-report.md`
  - `docs/lighthouse-report.md`
  - `docs/alpha-exposure-policy.md`
  - `docs/bug-check-2026-06-06.md`
  - `docs/eh-universe-baseline-checklist.md`
  - `docs/github-release-v3.md`
  - `docs/patch-notes-v2.md`
  - `docs/patch-notes-v3.md`
  - `docs/RELEASE_NOTES_v2.2.0-alpha.1.md`

삭제 기준:

- 2026-06-10 이전 파일.
- 현재 Loreguard 기준 문서와 충돌하거나 삭제된 공개 표면(Code Studio, Network, Archive, Reports, Tools, Reference, Rulebook)을 활성 제품처럼 다룸.
- 실행 코드가 아니며, 현재 source-of-truth 문서가 아님.

보존 기준:

- `src/lib/writing-workspace/*`는 6월 10일 이전 파일이어도 신버전 집필 IDE에서 실제 사용되므로 삭제하지 않음.
- 사업/연구/디자인 문서 중 재료 가치가 남은 문서는 삭제하지 않음.
- 코드 주석이나 e2e에서 이름 참조가 남은 역사 문서는 다음 라운드 후보로 보류.

## 재생성 가능한 공간 차지 파일 삭제

2026-06-12 추가 정리:

- `.next` 삭제.
- `coverage` 삭제.
- `tsconfig.tsbuildinfo` 삭제.
- `.dev-3011.log` 삭제.
- `_evidence_screenshots` 삭제.
- `_modoo_capture` 삭제.
- `.codex-design-audit` 삭제.
- `.playwright-mcp` 삭제.

삭제 용량:

- 약 7.68GB.

보존:

- `node_modules`는 약 2.6GB지만 앱 실행 의존성이므로 이번 정리에서는 유지.
- 현재 소스, public asset, scripts, e2e, docs source-of-truth는 유지.
