# 예비설계안 세트 — 정합·정밀성 검토 결과 (2026-06-03)

> 대상: 본 폴더 7문서. 방법: Claude 직접 자기감사 (QA 감사원 **A 정합검사관** + **C 반증가** 적용, 발견만·수정은 별도). 핵심 주장은 glob/grep 실측 재검증.
> ⚠️ 정직(QA §6): 이 검토도 **같은 모델의 자기검토** = 진짜 외부성 X. 고위험 결정은 다른 모델/사람 cross-check 권고.

---

## 1. 검증 등급 (firsthand 여부)

| 문서 | 등급 | 근거 |
|---|---|---|
| `00-세계관` | **A** | 세계관 layer(터널_구조·정합매트릭스·체크리스트) 직접 정독. 단 분석 *전* 작성 → 정합모델 stale(수정함) |
| `02-씬시트·연출` | **A**(씬시트) / C(연출) | 씬시트 표준 직접 재검증 ✅ / 연출은 폴더 비어있음·instruction 추정 |
| `_공통-3기둥` | **A**(QA감사원) / B(권위본·영수증) | QA감사원 직접 정독 ✅ / 권위본·영수증·게이트는 Agent 5 |
| `01-캐릭터` | **B** | `_template_character.md` 실재 확인 ✅ — 단 내용은 Agent 1, 직접 미독 |
| `04-집필·퇴고·출고` | **B/C** | `_도구/` 소스 실재 / ep001은 `_archive/` Agent 3 보고, 직접 미독 |
| `03-구성` | **C (최약)** | tunnel-yuju 인스턴스 경로 **glob 0건** — Agent 4 경로 신뢰 불가 |

---

## 2. A 정합검사관 — 정합(coherence) findings

| # | 항목 | 판정 | 처리 |
|---|---|---|---|
| A1 | `00` PART2(단일 Auditor+N표) ↔ `_공통`(A/B/C/D 비수렴 패널) **모델 불일치** | 결함 | **수정** — 00에 "정본=공통 ②" 상향 note 삽입 |
| A2 | `_공통` 매핑이 `project-serializer.ts`(절대금지8) "경유"로 읽힘 = 수정금지 위반 소지 | 결함 | **수정** — "참조만·수정 X" + 신규 `serializer.ts` 명시 |
| A3 | README 커밋단위 표 ↔ 각 탭 doc | 일치 | OK |
| A4 | "−20점" 2종(매트릭스 합치기금지 vs 자수미달) 구분 — 공통/04 | 일관 | OK (재검증 반영됨) |
| A5 | eh-web 매핑 심볼 실재: `generateJsonViaSpark`✅ `tier-registry.ts`✅ `UserRoleContext`✅ `useContinuityCheck`✅ | 확인 | OK |

## 3. C 반증가 — 정밀성(precision) findings

| # | 항목 | 심각도 | 처리 |
|---|---|---|---|
| C1 | **`03` 메인스토리 소스 경로 glob 0건** (`메인스토리/*.md`·최상위 `*.md`·`_main_story*` 전부 0). Agent 4 인용(92KB 등) 경로·크기 신뢰 불가 | **높음** | **수정** — 03에 검증등급 C + "착수 전 firsthand 확인 필수" 명기 |
| C2 | firsthand 검증 **불균형** — 02·세계관·QA감사원만 A급. 01(내용 미독)·03(경로 broken)·04(미독)은 B/C인데 doc은 "직접 재검증 핵심 소스"로 동격 표기 | 중간 | **수정** — 검증 등급표(본 문서 §1)로 명시. 각 doc도 등급 반영 권고 |
| C3 | 정량 수치(KPIPA 1.35조·기억 decay %·자수 threshold·핀셋 정확도)는 Agent 2차 전언 — 미측정 | 중간 | 잔여 — "출처 인용·미측정" 표시 권고 |
| C4 | TS 스케치 **미정의 타입** doc 간 분산(`LockEvent`·`Block`·`EntryRef`·`ForeshadowEntry`·`InfoNecessityGate` 등) | 낮음 | 잔여 — 예비 단계 허용. 통합 시 `types` 단일화 |
| C5 | Agent 4 신뢰도 저하가 **03 전반(12§ 스키마)**에 전이 — 경로가 틀렸으니 12§도 의심 | 중간 | 잔여 — 12§는 실재 템플릿(`_메인스토리_구성.md`) 기반이라 plausible하나 인스턴스 미검증 |

---

## 4. 적용한 수정 (4건)
- **A1** `00-세계관`: 정합모델 상향 note (정본=`_공통` ②).
- **A2** `_공통-3기둥`: `project-serializer`=절대금지8 참조만, 신규 `serializer.ts` 명시.
- **C1** `03-구성`: 메인스토리 경로 ⚠️ 미검증 표기 + 검증등급 C.
- **C2** 본 `_검토_정합_정밀성.md` 신설 — 검증 등급표 단일 출처.

## 5. 잔여 권고 (착수 전)
1. **03-구성 firsthand 재검증 선결** — 실 메인스토리 인스턴스 위치 확인(재시작 `_archive/` 추정). 못 찾으면 12§ 스키마 자체 재도출.
2. **정량 수치 출처 표시** (C3) — 측정 스크립트 없는 수는 "인용·미측정"로 (M-02 교훈과 동일).
3. **types 단일화** (C4) — 구현 시 `worldgraph/types.ts`류로 공유 타입 모음.
4. **고위험 cross-check** (C5) — 파일럿 착수 전 다른 모델/사람 1패스.

---

## 6. 종합 판정

```
[예비설계안 정합·정밀성]
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  정합 (coherence)    ████████░░  8/10  (2 결함 수정 완료, 잔여 0)
  정밀성 (precision)  ██████░░░░  6/10  (firsthand 불균형 — 03 C급)
  소스 그라운딩        ███████░░░  7/10  (02·QA·세계관 견고 / 03 broken)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  판정: 예비설계로 타당 / 03(구성) firsthand 보강이 착수 선결
━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

- **견고**: 02-씬시트(8영역 직접 검증), _공통 ②(QA 감사원 직접 검증), 00-세계관(layer 직접). 이 셋은 바로 착수 가능 수준.
- **보강 필요**: 03-구성(경로 broken·등급 C). 01/04는 소스 실재하나 내용 firsthand 미확인(등급 B).
- **이번 검토의 가치**: Agent 4 경로 허위(메인스토리 92KB 등 0건)를 glob 실측으로 적발. 그대로 믿었으면 03이 잘못된 경로 위에 설계될 뻔. = MEMORY "Agent 결과물 직접 재검증" 원칙의 실증.

**전부 대기 유지.** 파일럿 = 세계관(00, A급) Phase 0 round-trip. 03은 firsthand 재검증 후 등급 상향 시 착수.
