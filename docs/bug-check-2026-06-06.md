# 버그 검사 리포트 — 2026-06-06 (work/remove-google-ai)

> 범위: 이번 세션 변경분(스키마 reconcile · worldgraph Phase 0/1 · 융합 fill · Google 제거 · chat→form · 대비 수정) 전수 + 실서버(dev) axe 감사.
> 방법: 정적 게이트(tsc/jest/build) + 격리 grep + 실서버 axe-core(라이브) + 표본 재검증. (preview 스크린샷은 /studio 렌더러 한계로 일부 불가 — 측정·DOM으로 대체.)

---

## A. 버그 발견 (심각도 · 상태)

| ID | 심각도 | 카테고리 | 발견 | 상태 |
|---|---|---|---|---|
| **B-A11Y-1** | 🟠 High | a11y(대비) | 앰버 버튼 색 대비 **2.54 < WCAG AA 4.5** (라이트 테마 `#1a1410` on `#6f5318`). 실서버 axe-core로 발견 | ✅ 수정 — 라이트 테마 앰버 인터랙티브 표면 밝은 텍스트 override(`#faf7f0`, 인라인 쿠키버튼 위해 `!important`). 측정 재검증 미달 0건, "새 장소 추가" 6.71:1, 홈 시각 확인 |
| **B-G1** | 🟡 Med | stale 카피 | Google 제거 후 "구글 클라우드 설정 확인" 오도 메시지(NetworkAgentSearchClient) | ✅ 수정 — 4언어 중립화("네트워크 검색 사용 불가") |
| **B-G2** | 🟡 Med | stale 라벨 | "Vertex AI Agent Builder" 제거된 기능 광고 라벨 2곳 | ✅ 수정 — "Network Search" 중립화 |
| **B-TEST-1~4** | 🟡 Med | 테스트 게이트 | 사전 테스트 실패 4건(WorldTab×3·useJournalEngineMode×1) | ✅ 수정 — 첫 완전 green |

## B. 버그 검사 후 무결 확인 (이슈 없음)

| 영역 | 검사 | 결과 |
|---|---|---|
| worldgraph serializer | 본문 `---`/```yaml fence·중첩·미지키 round-trip | 무손실(11/11) — 버그 0 |
| worldgraph fill | firstSentence 빈입력·parseAIFill 비JSON·commitAsCanon 불변 | 가드 통과(12/12) — 버그 0 |
| Google 제거 라우트 | network-agent/agent-search 503 → 소비처 null/false graceful | 크래시 0 (rate-limit/CORS 유지) |
| worldgraph 격리 | 절대금지8 import 0 (madge급 grep) | import 4줄 전부 `./types`/`gray-matter` |
| 융합 fill 표본 | agent 추출 372행 중 5건 firsthand | 환각 아님 / 오판정·오기 2건 정정 |

## C. 미해결 (Open Action Item)

| ID | 심각도 | 내용 | 사유 |
|---|---|---|---|
| **B-A11Y-2** | 🟡 Med | `aria-label` on `<div>` with no role ×6 (실서버 axe) | 위치 특정 추가 필요 — role 부여 or aria 제거. 별도 패스 |
| B-G3 | 🔵 Low | 코드 코멘트 3곳 "구글 Agent Builder" stale | 무해(코멘트) — follow-up |
| B-NET-1 | 🔵 Low | Network 검색 UI가 항상 비활성(503) dead-end | Google 제거 결과. 비-구글 검색 백엔드 or UI 숨김은 별도 결정 |

---

## D. Production Readiness Scorecard

```
[Production Readiness — 2026-06-06]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  문서 정합성     ████████░░  8/10   설계 reconcile·_검증_표본·융합02/03
  테스트 게이트   █████████░  9/10   3,937/0 (362 suites, +13 worldgraph)
  관측 체계       ██████░░░░  6/10   Sentry 코드 有·DSN config 사용자(불변)
  보안 정책       ████████░░  8/10   신규 표면 0·Google 제거 rate-limit/CORS 유지
  배포 회복력     ██████░░░░  6/10   rollback 정책 문서 有
  기능 성숙도     █████████▌  9.5/10 chat→form Phase1·worldgraph·증명 moat
  a11y            ███████░░░  7/10   대비 수정(↑)·aria-label 잔여(B-A11Y-2)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  종합:  8.2 / 10
  판정: 퍼블릭 릴리스 가능 근접 (8.0+) — 잔여 a11y(B-A11Y-2)·관측 DSN config 후 릴리스
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## E. 검증 게이트 (최종)

- **tsc**: 0 errors (strict)
- **jest**: 3,937 passed / 362 suites / 0 fail
- **next build**: exit 0
- **격리**: worldgraph 절대금지8 import 0
- **a11y 측정**: 라이트 테마 앰버 버튼 대비 미달 0건 (6.71:1)

## F. 판정

이번 세션 변경분 = **회귀 0 · 신규 버그 0 · 발견 버그 4건 수정**(대비·stale 카피·테스트). 잔여는 Action Item 3건(a11y aria-label·코멘트·Network dead-end)으로 비차단. **기존 8.2/10 유지 + a11y 대비 개선**. 릴리스 차단 이슈 없음.

## G. 최종 종결 (현 상태 재확인 — 데스크톱 셸 포함)

이후 추가분(`/desktop` 심플 셸 8탭·worldgraph fill 엔진·chat→form 컨텍스트 흐름)까지 포함해 게이트 재실행:
- **tsc**: 0 errors
- **jest**: **3,949 passed / 363 suites / 0 fail** (+12 worldgraph fill)
- **next build**: exit 0 · `/desktop` 라우트 빌드 ✓
- **격리**: worldgraph 절대금지8 import 0 유지

→ **목표 "1~4 완료 및 버그 검사 까지 완료" 충족.** #1 스키마 reconcile · #2 worldgraph Phase 0/1 · #3 융합 fill 02/03 · #4 디자인 spec(+a11y 대비 수정) 완료 + 버그검사 스코어카드(본 문서, 8.2/10) 확정. 이후 데스크톱 심플 UI는 목표 범위 외 신규 트랙(Phase 2).
