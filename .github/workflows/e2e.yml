# Playwright E2E CI 스켈레톤 (비활성 상태).
#
# 활성화 시점:
#   - chromium만으로도 실제 런타임 ~6~10분 예상 (build 포함).
#   - 활성화하려면 아래 `on:` 섹션의 주석을 해제하고, 필요 시 비용 고려해
#     pull_request 브랜치 필터(예: branches: [master])를 추가하세요.
#
# name: E2E
# on:
#   push:
#     branches: [master]
#   pull_request:
#     branches: [master]
#
# jobs:
#   e2e:
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Install Playwright browsers
#         run: npx playwright install --with-deps chromium
#
#       - name: Build (prod)
#         run: npm run build
#         env:
#           NODE_ENV: production
#
#       - name: Run E2E
#         run: npm run test:e2e
#         env:
#           CI: 'true'
#
#       - name: Upload Playwright report
#         if: ${{ !cancelled() }}
#         uses: actions/upload-artifact@v4
#         with:
#           name: playwright-report
#           path: playwright-report/
#           retention-days: 7
