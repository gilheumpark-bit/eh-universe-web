import { RuleDetector } from '../registry';
/**
 * Phase / Rule Category: async
 * addEventListener(리터럴 이벤트)는 있는데 같은 이벤트 문자열의 removeEventListener가 파일에 없음
 */
export declare const asy009Detector: RuleDetector;
