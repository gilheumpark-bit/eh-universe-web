import { RuleDetector } from '../registry';
/**
 * CMX-008: Cyclomatic Complexity 10 초과
 * Counts decision points (if, for, while, case, catch, &&, ||, ?:) per function.
 */
export declare const cmx008Detector: RuleDetector;
