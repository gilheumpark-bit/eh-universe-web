import { RuleDetector } from '../registry';
/**
 * CMX-010: 삼항 중첩 3단+
 * Detects ternary (conditional) expressions nested 3 or more levels deep.
 */
export declare const cmx010Detector: RuleDetector;
