import { RuleDetector } from '../registry';
/**
 * Phase / Rule Category: async
 * async 함수에서 if(조건) return 값; 만 있고 else 없이 뒤에 문이 이어지는 패턴 (경로별 반환 누락 가능)
 */
export declare const asy004Detector: RuleDetector;
